import 'package:flutter/material.dart';

class LeaveApprove extends StatelessWidget {
  const LeaveApprove({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}